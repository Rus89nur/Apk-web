# QA_WEB — чеклист приёмки веб-PWA «программа НЕТ»

Дата прогона: 03.06.2026  
Среда: HTTP (`python3 dev-server.py` → http://localhost:3000/), не `file://`

## Автотесты (Vitest)

Прогон 03.06.2026: **15/15 passed** (`npm test`).

- [x] backup-import — v1.3 поля, merge, round-trip, demo file, BOM
- [x] akt-utils — isDraft, nextAktNumber, occupiedNumbers по году

## Ручная приёмка

| # | Сценарий | Статус | Примечание |
|---|----------|--------|------------|
| 1 | Холодный старт — акт с нуля, 6 шагов, черновик и завершение | ☐ | |
| 2 | Импорт iOS: `assets/sample-demo.gazprombackup`, replace и merge; после F5 данные на месте | ☐ | `.gazprombackup` в accept; verify IDB |
| 3 | Фото нарушений: lightbox, экспорт акта, re-import | ☐ | |
| 4 | Word: шаблон .docx, генерация без ошибок XML | ☐ | Локальные vendor в `assets/vendor/` |
| 5 | Excel: реестр, история, **график** (кнопка в редакторе графика / плитка отчётов) | ☐ | |
| 6 | Устранение: срок, просрочка, фото до/после, bulk «все устранённые» | ☐ | Статистика на экране |
| 7 | Корзина: восстановление акта | ☐ | |
| 8 | PWA: установка, офлайн статика (SW v29 + vendor) | ☐ | SW отключён на localhost в app.js |
| 9 | Регрессия бэкапа v1.3: export → clear → import, counts | ☐ | `.gazprombackup`, поля registry/templates/word |

## Известные ограничения

- Service Worker на `localhost` намеренно не кэширует (см. `app.js`) — офлайн-PWA проверять на GitHub Pages / nginx.
- ML-плитка в настройках отключена: «Только iOS (архив)».
- Номер акта уникален в рамках **года** даты проверки (см. мастер, шаг 1).

## Баги, найденные при прогоне

| # | Описание | Статус |
|---|----------|--------|
| 1 | Пустая БД блокировала мастер — редирект на импорт бэкапа | **fixed** — мастер открывается с `emptyCatalog`, справочники пополняются в шагах |
| 2 | Импорт iOS | OK, без изменений |
| 3 | Word: `PIZZIP_CDN is not defined` | **fixed** — `generateFromAkt` использует `ensureLibs()` и `assets/vendor/pizzip.min.js` |
| 4 | «Очистить данные» не очищало UI/IDB | **fixed** — `clear()` пишет пустой каталог, чистит `photos`, `refreshAll` всегда перерисовывает экраны |
| 5 | Двойной round-trip бэкапа на шаге 6 | не требуется (кнопки нет) |
| 6 | Медленное переключение «устранено» | **fixed** — debounce сохранения, точечное обновление DOM без полного `refreshAll` |

_Исправления: 03.06.2026_